import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#51247a" }, // Màu tím đặc trưng UQ
    secondary: { main: "#962a8b" },
  },
});
