import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#51247a" }, // UQ signature purple
    secondary: { main: "#962a8b" },
  },
});
